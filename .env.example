# 🔑 OPENAI API KEY - Solo para desarrollo local (Godot Editor)
# Copia este archivo como .env y agrega tu API key real
# NUNCA subas el archivo .env a Git

# Para desarrollo local (Desktop):
OPENAI_API_KEY=sk-proj-tu-api-key-aqui

# ⚠️ NOTA: Para la versión web (GitHub Pages):
# Los usuarios deben configurar su propia API key usando el comando:
# /setkey sk-proj-tu-key-aqui
# 
# La key se guarda en localStorage del navegador y NO se envía a ningún servidor.
